{
  "name": "lingodash",
  "version": "1.0.0",
  "description": "AI-powered word definition lookup using Google Gemini",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest",
    "test:ui": "vitest --ui",
    "test:run": "vitest run",
    "coverage": "vitest run --coverage"
  },
  "dependencies": {},
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
